/**
 * Test file to verify icon imports
 */

import { LayoutDashboard, LayoutGrid } from 'lucide-react'

// Just a simple export to verify the imports work
export const TestIcons = {
  LayoutDashboard,
  LayoutGrid
} 